---
import ViewItem from "@components/ViewItem.astro";
import { data } from "@data/views";
import MainLayout from "@layouts/MainLayout.astro";
---

<MainLayout>
  {
    data.map((item) => {
      return <ViewItem {...item} />;
    })
  }
</MainLayout>
